"use strict"; // treat all JS code as newer version

// alert(3+3) we are using nodejs rather than browser version

console.log(3
    +
    3) // code readability should be high 

    console.log("Ali");
    

    let name = "Ali"
    let age = 18
    let isloggedin = true
    let state;

    // number => 2 to power 53 
    // bigint
    // string==>""
    // boolean ==> true/false
    // null ==> standalone value
    // undefined ==>
    // symbol ==> unique
    

    // object

    console.log(typeof age)
